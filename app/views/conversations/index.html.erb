<div class="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <h1 class="text-4xl font-bold mb-8 text-gray-900 dark:text-white">내 메시지함</h1>

  <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg p-8">
    <% if @conversations.any? %>
      <div class="space-y-4">
        <% @conversations.each do |conversation| %>
          <% recipient = conversation.sender == current_user ? conversation.recipient : conversation.sender %>
          <%= link_to conversation_path(conversation), class: "block p-4 rounded-lg shadow-md bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 transition duration-200" do %>
            <p class="text-lg font-semibold text-gray-900 dark:text-white">대화 상대: <%= recipient.email %></p>
            <% if conversation.messages.any? %>
              <p class="text-sm text-gray-600 dark:text-gray-300 mt-1">최근 메시지: <%= conversation.messages.last.body.truncate(50) %></p>
            <% end %>
          <% end %>
        <% end %>
      </div>
    <% else %>
      <p class="text-gray-600 dark:text-gray-300">아직 진행 중인 대화가 없습니다.</p>
    <% end %>
  </div>
</div>
