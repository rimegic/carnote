<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "A" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Alpine.js for interactive components -->
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
  </head>

  <body class="bg-white text-gray-900 min-h-screen">
    <!-- Header -->
    <header class="bg-white/95 backdrop-blur-md border-b border-gray-200 sticky top-0 z-50 shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 xl:px-12">
        <div class="flex justify-between items-center h-16 sm:h-18 lg:h-20">
          <!-- Logo -->
          <div class="flex-shrink-0">
            <%= link_to root_path, class: "flex items-center group" do %>
              <span class="text-xl sm:text-2xl lg:text-3xl font-bold text-blue-600 group-hover:text-blue-700 transition-colors duration-200">카노트</span>
              <span class="text-xs text-gray-500 ml-1 hidden sm:inline">CARNOTE</span>
            <% end %>
          </div>

          <!-- Desktop Navigation -->
          <nav class="hidden lg:flex space-x-1 xl:space-x-2">
            <%= link_to "신차견적", cars_path, class: "text-gray-700 hover:text-blue-600 hover:bg-blue-50 px-3 xl:px-4 py-2 xl:py-3 text-sm xl:text-base font-medium transition-all duration-200 rounded-lg" %>
            <%= link_to "비교견적", car_comparisons_path, class: "text-gray-700 hover:text-blue-600 hover:bg-blue-50 px-3 xl:px-4 py-2 xl:py-3 text-sm xl:text-base font-medium transition-all duration-200 rounded-lg" %>
            <%= link_to "금융상품", "#", class: "text-gray-700 hover:text-blue-600 hover:bg-blue-50 px-3 xl:px-4 py-2 xl:py-3 text-sm xl:text-base font-medium transition-all duration-200 rounded-lg" %>
            <%= link_to "캐시백", "#", class: "text-gray-700 hover:text-blue-600 hover:bg-blue-50 px-3 xl:px-4 py-2 xl:py-3 text-sm xl:text-base font-medium transition-all duration-200 rounded-lg" %>
            <%= link_to "이벤트", "#", class: "text-gray-700 hover:text-blue-600 hover:bg-blue-50 px-3 xl:px-4 py-2 xl:py-3 text-sm xl:text-base font-medium transition-all duration-200 rounded-lg" %>
          </nav>

          <!-- User Menu -->
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <!-- Notifications -->
              <%= link_to notifications_path, class: "relative p-2 text-gray-400 hover:text-gray-500" do %>
                <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-5 5-5-5h5v-12h5v12z" />
                </svg>
                <% if current_user.unread_notifications_count > 0 %>
                  <span class="absolute top-0 right-0 block h-2 w-2 rounded-full bg-red-400 ring-2 ring-white"></span>
                <% end %>
              <% end %>

              <!-- Logout Button -->
              <%= link_to destroy_user_session_path, 
                  data: { 
                    "turbo-method": :delete
                  }, 
                  class: "text-gray-700 hover:text-blue-600 px-3 py-2 text-sm font-medium transition-colors" do %>
                로그아웃
              <% end %>

              <!-- User Dropdown -->
              <div class="relative" x-data="{ open: false }">
                <button type="button" 
                        class="flex items-center text-sm rounded-full focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" 
                        id="user-menu-button"
                        @click="open = !open"
                        @click.away="open = false">
                  <span class="sr-only">Open user menu</span>
                  <div class="h-8 w-8 rounded-full bg-blue-100 flex items-center justify-center">
                    <span class="text-sm font-medium text-blue-600">
                      <%= current_user.name&.first&.upcase || current_user.email.first.upcase %>
                    </span>
                  </div>
                </button>
                
                <!-- Dropdown menu -->
                <div x-show="open" 
                     x-transition:enter="transition ease-out duration-100"
                     x-transition:enter-start="transform opacity-0 scale-95"
                     x-transition:enter-end="transform opacity-100 scale-100"
                     x-transition:leave="transition ease-in duration-75"
                     x-transition:leave-start="transform opacity-100 scale-100"
                     x-transition:leave-end="transform opacity-0 scale-95"
                     class="absolute right-0 z-10 mt-2 w-48 origin-top-right rounded-md bg-white py-1 shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none"
                     role="menu" 
                     aria-orientation="vertical" 
                     aria-labelledby="user-menu-button">
                  <%= link_to user_path, class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100", role: "menuitem" do %>
                    내 프로필
                  <% end %>
                  <%= link_to favorites_path, class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100", role: "menuitem" do %>
                    찜한 차량
                  <% end %>
                  <%= link_to price_alerts_path, class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100", role: "menuitem" do %>
                    가격 알림
                  <% end %>
                  <%= link_to conversations_path, class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100", role: "menuitem" do %>
                    메시지
                  <% end %>
                </div>
              </div>
            <% else %>
              <%= link_to "로그인", new_user_session_path, class: "text-gray-700 hover:text-blue-600 px-3 py-2 text-sm font-medium" %>
              <%= link_to "회원가입", new_user_registration_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md text-sm font-medium transition-colors" %>
            <% end %>
          </div>

          <!-- Mobile menu button -->
          <div class="lg:hidden" x-data="{ mobileMenuOpen: false }">
            <button type="button" 
                    class="inline-flex items-center justify-center p-2.5 rounded-xl text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-blue-500 transition-all duration-200 min-h-[44px] min-w-[44px] touch-manipulation"
                    @click="mobileMenuOpen = !mobileMenuOpen">
              <span class="sr-only">Open main menu</span>
              <!-- Hamburger icon -->
              <svg x-show="!mobileMenuOpen" class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
              </svg>
              <!-- Close icon -->
              <svg x-show="mobileMenuOpen" class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
            
            <!-- Mobile menu -->
            <div x-show="mobileMenuOpen" 
                 x-transition:enter="transition ease-out duration-300"
                 x-transition:enter-start="opacity-0 -translate-y-2"
                 x-transition:enter-end="opacity-100 translate-y-0"
                 x-transition:leave="transition ease-in duration-200"
                 x-transition:leave-start="opacity-100 translate-y-0"
                 x-transition:leave-end="opacity-0 -translate-y-2"
                 class="absolute top-16 sm:top-18 lg:top-20 left-0 right-0 bg-white/95 backdrop-blur-md shadow-xl border-t border-gray-200 z-50"
                 @click.away="mobileMenuOpen = false">
              <div class="px-4 sm:px-6 py-6 sm:py-8 space-y-6">
                <!-- Navigation Links -->
                <div class="space-y-1">
                  <%= link_to "신차견적", cars_path, class: "block text-gray-700 hover:text-blue-600 hover:bg-blue-50 py-3 px-4 text-base font-medium rounded-xl transition-all duration-200 min-h-[48px] flex items-center touch-manipulation" %>
                  <%= link_to "비교견적", car_comparisons_path, class: "block text-gray-700 hover:text-blue-600 hover:bg-blue-50 py-3 px-4 text-base font-medium rounded-xl transition-all duration-200 min-h-[48px] flex items-center touch-manipulation" %>
                  <%= link_to "금융상품", "#", class: "block text-gray-700 hover:text-blue-600 hover:bg-blue-50 py-3 px-4 text-base font-medium rounded-xl transition-all duration-200 min-h-[48px] flex items-center touch-manipulation" %>
                  <%= link_to "캐시백", "#", class: "block text-gray-700 hover:text-blue-600 py-2 text-base font-medium" %>
                  <%= link_to "이벤트", "#", class: "block text-gray-700 hover:text-blue-600 py-2 text-base font-medium" %>
                </div>
                
                <div class="border-t border-gray-200 pt-4">
                  <% if user_signed_in? %>
                    <!-- User Info -->
                    <div class="flex items-center mb-4">
                      <div class="h-10 w-10 rounded-full bg-blue-100 flex items-center justify-center">
                        <span class="text-base font-medium text-blue-600">
                          <%= current_user.name&.first&.upcase || current_user.email.first.upcase %>
                        </span>
                      </div>
                      <div class="ml-3">
                        <p class="text-sm font-medium text-gray-700">
                          <%= current_user.name.present? ? current_user.name : "사용자" %>
                        </p>
                        <p class="text-xs text-gray-500"><%= current_user.email %></p>
                      </div>
                    </div>
                    
                    <!-- User Menu Items -->
                    <div class="space-y-2">
                      <%= link_to user_path, class: "block text-gray-700 hover:text-blue-600 py-2 text-base" do %>
                        내 프로필
                      <% end %>
                      <%= link_to favorites_path, class: "block text-gray-700 hover:text-blue-600 py-2 text-base" do %>
                        찜한 차량
                      <% end %>
                      <%= link_to price_alerts_path, class: "block text-gray-700 hover:text-blue-600 py-2 text-base" do %>
                        가격 알림
                      <% end %>
                      <%= link_to conversations_path, class: "block text-gray-700 hover:text-blue-600 py-2 text-base" do %>
                        메시지
                      <% end %>
                      <%= link_to notifications_path, class: "block text-gray-700 hover:text-blue-600 py-2 text-base" do %>
                        알림
                        <% if current_user.unread_notifications_count > 0 %>
                          <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-red-100 text-red-800 ml-2">
                            <%= current_user.unread_notifications_count %>
                          </span>
                        <% end %>
                      <% end %>
                    </div>
                    
                    <div class="border-t border-gray-200 mt-4 pt-4">
                      <%= link_to destroy_user_session_path, 
                          data: { 
                            "turbo-method": :delete
                          }, 
                          class: "block w-full text-left bg-red-50 text-red-700 hover:bg-red-100 py-2 px-3 rounded-md text-base font-medium" do %>
                        로그아웃
                      <% end %>
                    </div>
                  <% else %>
                    <div class="space-y-3">
                      <%= link_to "로그인", new_user_session_path, class: "block w-full text-center bg-white border border-gray-300 text-gray-700 hover:bg-gray-50 py-2 px-4 rounded-md text-base font-medium" %>
                      <%= link_to "회원가입", new_user_registration_path, class: "block w-full text-center bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded-md text-base font-medium" %>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </header>
    
    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-md mx-4 mt-4" role="alert">
        <span class="block sm:inline"><%= notice %></span>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-md mx-4 mt-4" role="alert">
        <span class="block sm:inline"><%= alert %></span>
      </div>
    <% end %>

    <!-- Main Content -->
    <main>
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white py-12 sm:py-16 lg:py-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 xl:px-12">
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-12">
          <div class="col-span-1 sm:col-span-2 lg:col-span-2">
            <h3 class="text-xl sm:text-2xl lg:text-3xl font-bold text-blue-400 mb-4 sm:mb-6">카노트</h3>
            <p class="text-gray-300 mb-6 sm:mb-8 text-sm sm:text-base lg:text-lg leading-relaxed">
              <span class="block">신차 구매의 새로운 기준</span>
              <span class="block mt-1">투명한 견적 비교와 최적의 금융 상품을 제공합니다.</span>
            </p>
            <div class="flex space-x-4">
              <a href="#" class="text-gray-400 hover:text-white">
                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M24 4.557c-.883.392-1.832.656-2.828.775 1.017-.609 1.798-1.574 2.165-2.724-.951.564-2.005.974-3.127 1.195-.897-.957-2.178-1.555-3.594-1.555-3.179 0-5.515 2.966-4.797 6.045-4.091-.205-7.719-2.165-10.148-5.144-1.29 2.213-.669 5.108 1.523 6.574-.806-.026-1.566-.247-2.229-.616-.054 2.281 1.581 4.415 3.949 4.89-.693.188-1.452.232-2.224.084.626 1.956 2.444 3.379 4.6 3.419-2.07 1.623-4.678 2.348-7.29 2.04 2.179 1.397 4.768 2.212 7.548 2.212 9.142 0 14.307-7.721 13.995-14.646.962-.695 1.797-1.562 2.457-2.549z"/>
                </svg>
              </a>
              <a href="#" class="text-gray-400 hover:text-white">
                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M22.46 6c-.77.35-1.6.58-2.46.69.88-.53 1.56-1.37 1.88-2.38-.83.5-1.75.85-2.72 1.05C18.37 4.5 17.26 4 16 4c-2.35 0-4.27 1.92-4.27 4.29 0 .34.04.67.11.98C8.28 9.09 5.11 7.38 3 4.79c-.37.63-.58 1.37-.58 2.15 0 1.49.75 2.81 1.91 3.56-.71 0-1.37-.2-1.95-.5v.03c0 2.08 1.48 3.82 3.44 4.21a4.22 4.22 0 0 1-1.93.07 4.28 4.28 0 0 0 4 2.98 8.521 8.521 0 0 1-5.33 1.84c-.34 0-.68-.02-1.02-.06C3.44 20.29 5.7 21 8.12 21 16 21 20.33 14.46 20.33 8.79c0-.19 0-.37-.01-.56.84-.6 1.56-1.36 2.14-2.23z"/>
                </svg>
              </a>
            </div>
          </div>

          <div>
            <h4 class="text-lg font-semibold mb-4">서비스</h4>
            <ul class="space-y-2 text-gray-300">
              <li><%= link_to "신차견적", cars_path, class: "hover:text-white" %></li>
              <li><a href="#" class="hover:text-white">비교견적</a></li>
              <li><a href="#" class="hover:text-white">금융상품</a></li>
              <li><a href="#" class="hover:text-white">오토캐시백</a></li>
            </ul>
          </div>

          <div>
            <h4 class="text-lg font-semibold mb-4">고객지원</h4>
            <ul class="space-y-2 text-gray-300">
              <li><a href="#" class="hover:text-white">공지사항</a></li>
              <li><a href="#" class="hover:text-white">자주묻는질문</a></li>
              <li><a href="#" class="hover:text-white">1:1 문의</a></li>
              <li><a href="#" class="hover:text-white">이용약관</a></li>
            </ul>
          </div>
        </div>

        <div class="border-t border-gray-700 mt-8 pt-8 text-center text-gray-400">
          <p>&copy; <%= Time.current.year %> 카노트(CARNOTE). All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
